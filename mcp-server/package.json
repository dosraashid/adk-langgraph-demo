{
  "name": "do-mcp-remote-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx @digitalocean/mcp --transport sse --services apps,droplets,databases --port 8080 --host 0.0.0.0"
  },
  "dependencies": {
    "@digitalocean/mcp": "latest"
  }
}
